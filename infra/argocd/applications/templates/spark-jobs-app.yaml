apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: spark-jobs-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: default
  source:
    repoURL: {{ .Values.git.repoUrl | quote }}
    targetRevision: {{ .Values.git.revision | quote }}
    path: infra/workloads/spark-apps/rbac
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: data-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
